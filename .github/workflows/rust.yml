# Superseded by ci.yml (host + kernel PR checks on master/devel).
# Kept as a thin alias so existing badge/links keep working.
name: Rust

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the CI workflow (.github/workflows/ci.yml) for PR build checks."
