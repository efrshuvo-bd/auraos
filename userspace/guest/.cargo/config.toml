# Guest EL0 binaries link at 0x400000 via user.ld.
# Portable defaults for Linux/CI. On Windows WDAC, run scripts/fix-linker.ps1.
[target.aarch64-unknown-none]
linker = "rust-lld"
rustflags = [
  "-C", "link-arg=-Tuser.ld",
  "-C", "link-arg=--no-eh-frame-hdr",
]
