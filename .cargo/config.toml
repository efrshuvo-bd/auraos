# Workspace-level cargo config.
# aarch64-unknown-none linker flags live in kernel/.cargo/config.toml (and
# userspace/guest/.cargo/config.toml) so guest EL0 builds do not pick up the
# kernel linker script.
